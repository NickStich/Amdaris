import { NumberABSPipe } from './number-abs.pipe';

describe('NumberABSPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberABSPipe();
    expect(pipe).toBeTruthy();
  });
});
