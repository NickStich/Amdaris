import { ThirdPartyPersonTypePipe } from './third-party-person-type.pipe';

describe('ThirdPartyPersonTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ThirdPartyPersonTypePipe();
    expect(pipe).toBeTruthy();
  });
});
